# return-file-contents-node

Simple express node server that returns the contents of the files inside data

# Run

```bash
npm install
npm run dev 
```

# Example calls

```
http://localhost:8080/csv/sheet.csv
http://localhost:8080/hello.txt
```
